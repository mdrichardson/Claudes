name: Release (personal)

on:
  push:
    tags:
      - 'v*-personal.*'

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install

      - name: Build Windows installer
        run: npx electron-builder --win --publish never -c.publish.owner=mdrichardson

      - uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: |
            dist/Claudes-Setup-*.exe
            dist/Claudes-Setup-*.exe.blockmap
            dist/latest.yml

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install

      - name: Build macOS installer
        run: npx electron-builder --mac --publish never -c.publish.owner=mdrichardson

      - uses: actions/upload-artifact@v4
        with:
          name: macos-artifacts
          path: |
            dist/Claudes-*.dmg
            dist/Claudes-*.zip
            dist/Claudes-*.zip.blockmap
            dist/latest-mac.yml

  release:
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List artifacts
        run: ls -la artifacts/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/*
